spring:
  datasource:
    url: jdbc:mysql://localhost:3310/cabinet_medical
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
